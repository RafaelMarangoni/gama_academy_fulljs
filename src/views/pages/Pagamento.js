import Nav from '../components/Nav'


let pagamento = {
    render : async () => {        
        let NavBar = await Nav.render();
        let view = ``
                
        return view 
    },
    after_render: async () => {
        
    }
}


export default pagamento;